# MySQL Production Profile
# 启用方式: --spring.profiles.active=mysql
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/crossdomaindb?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root123

  h2:
    console:
      enabled: false

  sql:
    init:
      mode: never
